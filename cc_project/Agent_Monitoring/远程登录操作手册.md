# 远程登录与编译验证操作手册（简版）

## 1. 远程登录

```bash
ssh yunxigu@192.168.2.66
pwd:yunxigu2025
cd /Users/yunxigu/cc_project/Agent_Monitoring
```

## 2. 操作验证尽量在远程机器的docker环境中调试

## 3. 开发模式部署（禁止重建镜像）
日常调试**禁止使用 `--build`**，必须通过 volume 挂载代码到容器：

## 4. 仅在以下情况允许重建镜像

- 修改了 `requirements.txt`（新增/删除依赖）
- 修改了 `Dockerfile`
- 首次部署（容器从未构建过）
